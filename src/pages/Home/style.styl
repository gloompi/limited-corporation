@import '../../assets/styles/main'

.home__wrap
  display: flex
  flex-direction: column
  align-items: center
  padding-top 30px

.home__title
  Font(light)
  font-size 50px
  color: $white
  letter-spacing 1.5px

.home__longtitle
  Font(medium)
  font-size 20px
  color: $white
  letter-spacing 1.2px